<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ink.whi.service.user.repo.mapper.UserFootMapper">

    <select id="countArticleByArticleId" resultType="ink.whi.api.model.vo.user.dto.ArticleFootCountDTO">
        select sum(if(f.praise_stat = ${@ink.whi.api.model.enums.PraiseStatEnum@PRAISE.code}, 1, 0))             as praiseCount,
               sum(if(f.collection_stat = ${@ink.whi.api.model.enums.CollectionStatEnum@COLLECTION.code}, 1,0))  as collectionCount
        from user_foot f
        where f.document_id = #{articleId}
    </select>

    <select id="countArticleReadsByUserId" resultType="java.lang.Integer">

    </select>
    <select id="countArticleByUserId" resultType="ink.whi.api.model.vo.user.dto.ArticleFootCountDTO">

    </select>
</mapper>
