image:
  abs-tmp-path: /tmp/sai-forum/
  web-img-path: /images/
  tmp-upload-path: /tmp/sai-forum/
  cdn-host:
  oss:
    type: local
    prefix:

spring:
  web:
    resources:
      static-locations:
        - classpath:/static/
        - file:${image.abs-tmp-path}
